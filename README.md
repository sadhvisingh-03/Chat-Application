# Chat-Application
This project is a Java-based Chat Application that allows multiple users to send and receive messages in real-time using client-server communication with sockets. It includes login authentication using MySQL database and a GUI built with Java Swing. The system uses OOP concepts and multithreading to handle multiple clients at the same time.
